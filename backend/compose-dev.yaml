services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: jlpt_mono
      POSTGRES_USER: jlpt
      POSTGRES_PASSWORD: jlpt_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
