import 'package:flutter/material.dart';

abstract final class AppColors {
  // Backgrounds
  static const cream = Color(0xFFFAF6F0);
  static const warmWhite = Color(0xFFFFFDF9);

  // Primary — terracotta/brown
  static const terracotta = Color(0xFFBE6A46);
  static const terracottaLight = Color(0xFFD4916F);
  static const terracottaDark = Color(0xFF8E4A2E);

  // Secondary — muted sage green
  static const sage = Color(0xFF8FA68E);
  static const sageMuted = Color(0xFFB5C7B4);

  // Neutrals
  static const textPrimary = Color(0xFF2D2420);
  static const textSecondary = Color(0xFF6B5E57);
  static const textHint = Color(0xFF9E9490);
  static const divider = Color(0xFFE5DDD6);

  // Semantic
  static const error = Color(0xFFBE4646);
  static const success = Color(0xFF5E8E5A);
}
